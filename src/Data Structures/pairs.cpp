// Combine together 2 values that may be different data types
// Way to 2 store 2 values as a single unit
// Basically a tuple, but can only store 2 values

#include <iostream>
#include <string>
#include <array>

using namespace std;

int main()
{
    array<int, 10> languages;

    for (int i = 0; i < languages.size(); i++)
    {
        languages[i] = pair<string, int> ()
    }   
    pair<string, int> 
}