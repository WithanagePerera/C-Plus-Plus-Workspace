// Bitmasking is when an integer is used for its binary representation
// It's used to represent a subset
// Say you 8 bits, the n'th bit refers to the n'th element in a subset
// So if that bit is on or off, that translates to the element in the subset