#include <iostream>

using namespace std;

static const float base_fine = 60.25;

int main()
{

}

void logInfo()
{}

float calculateFine(int speed)
{
    int speedLimit = 
    if (speed > speedLimit)
    {
        if (speed < speedLimit + 5)
            return base_fine;
        else if (speed < speedLimit + 10)
            return base_fine*1.05;
        else if (speed < speedLimit + 20)                                                                                                                                                                                                                                                                                                                                                                                                                                                                               )
            return base_fine*1.1;
        else
            reutn base_fine*1.2;
    }
    else
    {
        return 0;
    }
}